{
  "name": "my-app",
  "version": "1.0.0",
  "scripts": {
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend && npm run strapi develop",
    "dev": "concurrently \"npm dev frontend\" \"npm dev backend\""
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}
